// import { Action } from "../context/quixContext"
// import { Quiz } from "../data/quiz.types"

// export const isAnswerCorrect = (attemptedQuestionObj : Quiz, currentQuestion : number, action : Action) => {  
//   return attemptedQuestionObj.questions[currentQuestion + 1].answers.find((answer) => answer.id === action.payload && answer.isRight === true)!== undefined
// }

export {}